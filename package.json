{
  "name": "retro-ball",
  "private": true,
  "version": "1.0.0",
  "description": "Retro Ball \u2014 a synthwave isometric marble obstacle game",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-strip-types --test tests/*.test.mjs",
    "test:browser": "node scripts/check-browser.mjs",
    "benchmark:browser": "node scripts/benchmark-browser.mjs",
    "test:editor": "node scripts/check-editor.mjs"
  },
  "dependencies": {
    "@dimforge/rapier3d-compat": "^0.20.0",
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@types/three": "^0.185.4",
    "typescript": "^7.0.2",
    "vite": "^8.2.2"
  }
}
